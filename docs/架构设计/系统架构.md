---
title: 系统架构
description: MeetFlow 前端架构、组件层级和数据流
status: 已批准
created: 2026-04-09
updated: 2026-04-10
update_reason: 根据 web/src/ 代码结构填充
related:
  - docs/项目概述/项目总览.md
---

# 系统架构

## 架构概览

MeetFlow 是一个纯前端 Next.js 应用（含 API Routes），采用组件化架构。

```
web/src/
├── app/               # Next.js App Router
│   ├── page.tsx       # 主页面 (MeetFlowApp 组件)
│   ├── layout.tsx     # 根布局
│   └── api/           # API Routes
│       ├── ai/        # LLM 对话接口
│       └── asr/       # 语音识别接口
├── components/        # UI 组件
│   ├── layout/        # 布局组件 (Header, Sidebar)
│   ├── canvas/        # 画布编辑器 (4 种模式)
│   ├── voice/         # 语音面板
│   ├── ai/            # AI 功能
│   └── ui/            # shadcn/ui 基础组件
├── hooks/             # 自定义 Hook
└── lib/               # 工具函数
```

## 核心组件

### 页面布局

MeetFlowApp 是顶层组件，管理全局状态：

- `canvasMode`: 当前画布模式 (outline / mindmap / whiteboard / template)
- `aiChatOpen`: AI 侧边栏开关
- `voicePanelWidth`: 语音面板宽度

布局结构：`Header → [Sidebar | CanvasArea | VoicePanel | AIChatSidebar]`

### 画布模块 (`components/canvas/`)

| 组件 | 功能 |
|------|------|
| CanvasArea | 画布容器，按 mode 切换子组件 |
| OutlineEditor | 大纲编辑器（富文本） |
| MindMapEditor | 思维导图（simple-mind-map） |
| WhiteboardEditor | 白板绘图（Excalidraw） |
| TemplateEditor | 模板系统（SWOT 等预设模板） |

### 语音模块 (`components/voice/`)

| 组件 | 功能 |
|------|------|
| VoicePanel | 语音录制面板，含波形显示和 ASR 转写 |
| SummaryDialog | 录音总结弹窗 |

### AI 模块 (`components/ai/`)

| 组件 | 功能 |
|------|------|
| AIChatSidebar | 侧边栏对话（支持 streaming） |
| AIPanel | 底部快捷操作面板 |

### 布局模块 (`components/layout/`)

| 组件 | 功能 |
|------|------|
| Header | 顶部导航栏 |
| Sidebar | 左侧模式切换栏 |

## 数据流

```
用户操作
  ↓
组件状态 (React useState)
  ↓ 画布内容 / 语音输入
  ↓
API Routes (/api/ai, /api/asr)
  ↓
外部服务 (云雾 LLM / DashScope ASR)
  ↓
streaming 响应 → 更新 UI
```

## 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `1` | 切换到大纲模式 |
| `2` | 切换到思维导图 |
| `3` | 切换到白板 |
| `4` | 切换到模板 |
| `⌘/` | 打开/关闭 AI 助手 |

## 后端架构

当前后端仅有日志系统模块 (`src/utils/logging_config.py`)。主要功能通过 Next.js API Routes 实现。

详细日志规范见 [日志系统](../功能模块/日志系统.md)。
