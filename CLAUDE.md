# Auto-Picture 项目规则

## 黄金法则

### 1. 知识库优先 — 先读再做

开始任何任务前，必须先查阅知识库：

- 通读 `docs/INDEX.md` 获取全局视图
- 查看 `docs/开发计划/` 下最近日期的修复/开发记录
- 针对具体模块阅读对应的功能文档
- 搜索历史修复记录，避免重复踩坑

**快速导航：**

| 内容 | 路径 |
|------|------|
| 知识库总索引 | `docs/INDEX.md` |
| 项目总览 | `docs/项目概述/项目总览.md` |
| 运行环境 | `docs/项目概述/运行环境.md` |
| 系统架构 | `docs/架构设计/系统架构.md` |
| 大模型配置 | `etc/README.md` |

### 2. 知识库更新规则 — 改完代码必须更新文档

每次修改代码后，必须同步更新知识库，具体要求：

- **新增 Bug 修复** → 在 `docs/开发计划/` 下创建 `<M.DD>修复<序号>/fix_plan.md`，记录问题、原因、方案、影响文件、测试结果
- **新增/修改功能** → 更新对应的功能模块文档
- **架构变动** → 更新 `docs/架构设计/` 下相关文档，并同步更新 `docs/INDEX.md`
- **配置变更** → 更新 `etc/README.md`

### 3. Memory 与知识库统一规则

Memory 和知识库是相辅相成的，不能只更新其中一个：

- **记录到 Memory 的内容**：用户偏好、协作反馈、项目动态、外部资源引用等不适合放在代码仓库中的信息
- **记录到知识库的内容**：架构设计、功能说明、开发记录、配置指南等与项目直接相关的技术文档
- **同步原则**：当一条信息同时涉及 Memory 和知识库时，两边都要更新。例如：一次架构决策既要在知识库记录技术方案，也要在 Memory 记录决策背景和用户意图
- **交叉引用**：Memory 中可以指向知识库的具体文档路径，知识库中也可以注明"详见 Memory"以避免重复

## 项目结构

```
auto-picture/
├── CLAUDE.md          # 项目规则（本文件）
├── docs/              # 知识库
│   ├── INDEX.md       # 知识库总索引
│   ├── README.md      # 知识库使用说明
│   ├── 项目概述/       # 项目背景、目标、环境
│   ├── 架构设计/       # 系统架构、Agent 体系、协议设计
│   ├── 功能模块/       # 各功能模块详细文档
│   ├── 部署运维/       # 部署、配置、运维指南
│   └── 开发计划/       # 按日期归档的开发/修复记录
├── etc/               # 大模型及项目配置
├── src/               # 后端主程序
├── web/               # 前端服务
└── tests/             # 测试文件
```
