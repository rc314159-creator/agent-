# 配置说明

## 目录用途

`etc/` 目录用于存放大模型及项目的基本配置文件。

## 配置文件

| 文件 | 说明 |
|------|------|
| `config.example.yaml` | 配置模板（不含敏感信息，可提交到 Git） |
| `config.yaml` | 实际配置（包含 API Key，已加入 .gitignore） |

## 配置项说明

*待补充 — 随项目开发逐步完善。*
